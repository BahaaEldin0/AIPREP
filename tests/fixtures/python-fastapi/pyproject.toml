[project]
name = "fastapi-fixture"
version = "0.1.0"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.0",
]

[tool.poetry.dependencies]
python = "^3.11"
pytest = "^8.0"
